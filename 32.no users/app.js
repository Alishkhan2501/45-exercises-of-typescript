var users = ["Taha", "Mamoor", "Hamza", "Ayan", "Admin"];
if (users.length === 0) {
    console.log("We need to find some users!");
}
else {
    users = [];
    console.log("All users have been removed" + users.length);
}
